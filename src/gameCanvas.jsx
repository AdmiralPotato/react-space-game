var React = require('react');
var ReactDOM = require('react-dom');

var GameCanvas = (props) => {
	return (
		<div className="GameCanvas">
			OMG SVG ALL UP IN HERE
		</div>
	);
};

module.exports = GameCanvas;
